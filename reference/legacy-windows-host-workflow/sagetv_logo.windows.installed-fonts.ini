; Windows installed-font helper config
; Use this when the M PLUS Rounded 1c fonts are already installed in Windows.
; The run CMD also passes --disable-local-fonts, so the generator uses
; the installed fonts instead of the local M_PLUS_Rounded_1c folder.

[general]
output_root = generated

[fonts]
enabled = no
